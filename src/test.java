import java.util.Random;

public class test{
    public static void main(String[] args) {
        Ware ware = new Ware(null, 0, 0);

        Random r = new Random();
        System.out.println("Hallo\b\b\bSSS");


        System.out.println(r.nextInt(2));

            /* Falls noch mehr Essen gewünscht ist die nötige Arraylist und einige Funktionen                    

            private ArrayList<Ware> warenlisteEssen;

            warenlisteEssen = new ArrayList<>();

            spiel.warenlisteEssen.add(bohnenmitSpeckWare);

            for (int i = warenlisteStadt.size(); i < warenlisteStadt.size()+warenlisteEssen.size(); i++) {
                    System.out.format("%2$d%-14s", ") " + warenlisteEssen.get(i).getName(), i + 1);
                    System.out.println("|" + warenlisteEssen.get(i).getPreis() + "\t\b|" +
                                            warenlisteEssen.get(i).getGewicht());
            }
            System.out.println(warenlisteStadt.size()+warenlisteEssen.size() + ") Kein Interesse");
                
                

            */
    }
}